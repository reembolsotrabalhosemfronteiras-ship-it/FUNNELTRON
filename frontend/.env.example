# Copie para `.env.local` (que está no .gitignore) e ajuste.

# Dados de exemplo ligados por padrão. Ponha `false` só quando o backend estiver
# de pé (uvicorn na 8000) e com as chaves do Supabase em `backend/.env`.
VITE_USE_MOCK=true
