# Pins mínimos, não exatos. Os exatos (fastapi==0.115.0, pydantic==2.9.2, …)
# eram de 2024 e não têm wheel para Python 3.13+: o pip caía para compilar
# pydantic-core (Rust), Pillow e greenlet (C) na máquina, o que exige o
# Build Tools do Visual Studio. Com faixa aberta o pip pega wheel pronto.
fastapi>=0.115
uvicorn[standard]>=0.32
python-dotenv>=1.0
supabase>=2.9
pydantic>=2.9
# EmailStr (usado no cadastro/login) só valida com este pacote — sem ele o
# import do app inteiro quebra, não só a rota de auth.
email-validator>=2.2
pydantic-settings>=2.5
httpx>=0.27
python-multipart>=0.0.12
Pillow>=11
# Travado no exato: a imagem base (mcr.microsoft.com/playwright/python:v1.48.0-jammy)
# só tem os binários do Chromium da versão 1.48.0 pré-instalados. Com
# `>=1.48` o pip puxava a última versão (1.58 e subindo) e o pacote python
# não achava o executável do navegador — captura de print e "Sincronizar"
# quebravam em produção, mesmo funcionando local (onde o venv tinha
# `playwright install` rodado manualmente para a versão nova).
# Local: versão aberta para pegar wheel pronto no Python 3.14 (greenlet 3.1.1
# não tem wheel e exige Build Tools). Em produção o Docker continua com 1.48.0.
playwright>=1.48.0
# Notificação push (PIX gerado/pago) no navegador/SO via Web Push (VAPID).
pywebpush>=2.0
# Cache TTL em memória (usado no clarity_service para evitar chamadas repetidas à API).
cachetools>=5.3
